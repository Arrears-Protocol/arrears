{
  "name": "arrears",
  "private": true,
  "workspaces": ["web"],
  "scripts": {
    "build": "npm run build --workspace web",
    "dev": "npm run dev --workspace web",
    "verify:demo": "npm --prefix demo run verify",
    "test:contracts": "cd contracts && forge test --summary"
  },
  "dependencies": {
    "next": "^15.1.3"
  }
}
