{
  "name": "arrears",
  "private": true,
  "description": "Convenience scripts only. Deliberately declares no workspaces: Vercel builds with Root Directory web/, and a workspace parent would make npm refuse to install from inside it.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "verify:demo": "npm --prefix demo run verify",
    "test:contracts": "cd contracts && forge test --summary"
  }
}
